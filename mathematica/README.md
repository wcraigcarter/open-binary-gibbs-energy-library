# Mathematica

The initial reference implementation for reading and using the library data will live here.

The target interface is based on explicit, composable functions such as:

```wolfram
gMix["Cu-Ag"][temperature][composition]
```

The visualization application in the `Phase_Diagram_Tool` repository is a separate project and is not part of this library.
